import { View, Text } from "@ant-design/react-native";
import globalStyles from "../styles/global";

export default function HomeScreen() {
  return (
    <View style={globalStyles.container}>
      <Text>To do: Home</Text>
    </View>
  );
}
