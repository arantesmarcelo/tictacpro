import { Text, View } from "react-native";
import globalStyles from "../styles/global";

export default function Play() {
  return (
    <View style={globalStyles.container}>
      <Text>To do: Play</Text>
    </View>
  );
}