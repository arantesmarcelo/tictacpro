import { Text, View } from 'react-native';
import globalStyles from '../styles/global';

export default function Rules() {
  return (
    <View style={globalStyles.container}>
      <Text>To do: Rules</Text>
    </View>
  );
}
